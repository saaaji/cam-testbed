scene:
  name: calibration_rig

cameras:
  - name: left
    serial: 2228430D
    resolution: [1280, 720]
    
    # only need fx, fy, cx, and cy
    sparse_intrinsics: [1032.0, 1032.0, 640, 360]

    # distortion coefficients: (k1, k2, p1, p2[, k3[, k4, k5, k6]])
    distortion_coeff: [-0.33, 0.12, 0.0001, -0.0005, -0.0227]

